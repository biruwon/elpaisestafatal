import { rankMaterializationCandidates } from './materialization-candidates.mjs';

const candidates = rankMaterializationCandidates([
  { id: 'popular', text: 'España recicla menos vidrio', signature: 'espana recicla menos vidrio', count: 7, count7d: 4, growthRate: 1.33, priorityScore: 10, sourceIds: ['e1'], coverageStatus: 'partial', reason: 'Tiene fuentes candidatas.' },
  { id: 'rare', text: 'Pregunta rara', signature: 'pregunta rara', count: 2, priorityScore: 99, sourceIds: ['e2'] },
  { id: 'no-source', text: 'Sin fuente', signature: 'sin fuente', count: 8, priorityScore: 20, sourceIds: [] },
], { minCount: 3, max: 10 });
if (candidates.length !== 1 || candidates[0].clusterId !== 'popular') throw new Error('candidate ranking did not enforce popularity and source gates');
if (candidates[0].reviewStatus !== 'needs_review' || !candidates[0].suggestedSlug || candidates[0].requiredActions.length < 3 || candidates[0].count7d !== 4 || candidates[0].growthRate !== 1.33 || !candidates[0].reason) throw new Error('candidate lacks review metadata');
if (rankMaterializationCandidates([{ id: 'covered', text: 'Ya cubierta', count: 8, sourceIds: ['e3'], coverageStatus: 'covered' }]).length !== 0) throw new Error('covered cluster bypassed materialization gate');
if (rankMaterializationCandidates([{ id: 'newly-covered', text: 'Cobertura nueva', count: 8, sourceIds: ['e4'], coverageStatus: 'covered', newlyCovered: true }]).length !== 1) throw new Error('newly covered cluster was lost from review queue');
if (rankMaterializationCandidates([{ id: 'noisy', text: 'Audio transcription unavailable', count: 80, sourceIds: ['e5'], coverageStatus: 'partial', reviewable: false }]).length !== 0) throw new Error('non-reviewable cluster entered materialization queue');
if (rankMaterializationCandidates([{ id: 'published-alias', text: 'precios vivienda España', count: 8, sourceIds: ['e6'], coverageStatus: 'uncovered' }]).length !== 0) throw new Error('published claim alias remained in materialization queue');
if (rankMaterializationCandidates([{ id: 'published-alias-2', text: 'porcentaje personas soporta sobrecarga coste vivienda', count: 8, sourceIds: ['e7'], coverageStatus: 'partial' }]).length !== 0) throw new Error('published housing coverage remained unresolved');
if (rankMaterializationCandidates([{ id: 'warehouse-routable', text: 'crecimiento interanual PIB real España', count: 8, sourceIds: ['e8'], coverageStatus: 'uncovered' }]).length !== 0) throw new Error('warehouse-routable metric entered the editorial materialization queue');
if (rankMaterializationCandidates([{ id: 'warehouse-health-routable', text: 'cuanto gasta sanidad habitante espana', count: 8, sourceIds: ['e9'], coverageStatus: 'uncovered' }]).length !== 0) throw new Error('conversational health metric entered the editorial materialization queue');
if (rankMaterializationCandidates([{ id: 'compressed-published', text: 'espana paro alto europa', signature: 'comparative|polarity:positive|entity:employment|geo:espana|comparative:e', count: 8, sourceIds: ['e9'], coverageStatus: 'uncovered' }]).length !== 0) throw new Error('compressed wording for an existing published comparison entered the editorial materialization queue');
if (rankMaterializationCandidates([{ id: 'warehouse-arope-routable', text: 'porcentaje residentes arope espana', count: 8, sourceIds: ['e10'], coverageStatus: 'uncovered' }]).length !== 0) throw new Error('conversational AROPE metric entered the editorial materialization queue');
if (rankMaterializationCandidates([{ id: 'warehouse-population-routable', text: 'espana millones habitantes', count: 8, sourceIds: ['e11'], coverageStatus: 'uncovered' }]).length !== 0) throw new Error('compressed resident-population wording entered the editorial materialization queue');
if (rankMaterializationCandidates([{ id: 'warehouse-population-question-routable', text: 'cuantos habitantes viven normalmente espana', count: 8, sourceIds: ['e12'], coverageStatus: 'uncovered' }]).length !== 0) throw new Error('compressed resident-population question entered the editorial materialization queue');
if (rankMaterializationCandidates([{ id: 'warehouse-life-routable', text: 'evolucionado esperanza vida espana', count: 8, sourceIds: ['e12'], coverageStatus: 'uncovered' }]).length !== 0) throw new Error('compressed life-expectancy wording entered the editorial materialization queue');
if (rankMaterializationCandidates([{ id: 'warehouse-youth-routable', text: 'porcentaje poblacion menos anos', count: 8, sourceIds: ['e13'], coverageStatus: 'uncovered' }]).length !== 0) throw new Error('compressed young-population wording entered the editorial materialization queue');
if (rankMaterializationCandidates([{ id: 'legal-reuse-routable', text: 'la informacion publica se puede reutilizar sin condiciones', count: 8, sourceIds: ['boe-live-BOE-A-2007-19814'], coverageStatus: 'uncovered' }]).length !== 0) throw new Error('BOE legal-reuse wording entered the materialization queue');
if (rankMaterializationCandidates([{ id: 'budget-transfer-routable', text: 'educacion presidencia personal transferencia', count: 8, sourceIds: ['source-5f18c5ab47e36b2f'], coverageStatus: 'uncovered' }]).length !== 0) throw new Error('budget-transfer wording entered the manual materialization queue');
if (rankMaterializationCandidates([{ id: 'discovery-only', text: 'El gobierno paga periodistas para que callen una noticia', count: 80, sourceIds: ['lamoncloa-discovery-document-1'], coverageStatus: 'uncovered' }]).length !== 0) throw new Error('discovery-only lead entered the editorial materialization queue');
if (rankMaterializationCandidates([{ id: 'local-only', text: 'En mi barrio ha subido la inseguridad este mes', count: 80, sourceIds: ['source-e11'], coverageStatus: 'uncovered' }]).length !== 0) throw new Error('hyper-local claim entered the national materialization queue');
if (rankMaterializationCandidates([{ id: 'local-zone', text: 'La policía registra delitos en la zona', count: 80, sourceIds: ['source-e11'], coverageStatus: 'uncovered' }]).length !== 0) throw new Error('zone-specific claim entered the national materialization queue');
if (rankMaterializationCandidates([{ id: 'mixed-leads', text: 'España tiene más paro que la media europea', count: 8, sourceIds: ['source-e12', 'boe-discovery-document-2'], coverageStatus: 'partial' }]).length !== 0) throw new Error('published unemployment comparison remained in the materialization queue after semantic reconciliation');
console.log('Materialization candidate validation passed: popularity never bypasses review gates.');
