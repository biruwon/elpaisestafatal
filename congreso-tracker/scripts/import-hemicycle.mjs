import { writeFile } from 'node:fs/promises';
const url='https://www.congreso.es/es/hemiciclo';const html=await fetch(url,{headers:{'user-agent':'congreso-tracker/0.1 research contact'},signal:AbortSignal.timeout(30000)}).then(r=>r.text());const seats=[];const re=/<area[^>]+alt="([^"]+)"[^>]+coords="([^"]+)"[^>]+[\s\S]*?href="javascript:getUrlFichaDiputado\((\d+),\s*(\d+)\)/gi;let m;while((m=re.exec(html)))seats.push({officialId:m[3],legislature:m[4],label:m[1],coords:m[2].trim(),sourceUrl:url});await writeFile('data/hemicycle.json',JSON.stringify({meta:{retrievedAt:new Date().toISOString(),imageUrl:'https://www.congreso.es/o/diputados/img/hemiciclo.png',caveat:'Current seating map; do not apply retrospectively without dated evidence.'},seats},null,2));console.log(`indexed ${seats.length} hemicycle seats`);
