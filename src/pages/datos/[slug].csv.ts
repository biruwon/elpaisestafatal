import { concerns } from '../../data/concerns';
export function getStaticPaths(){return concerns.map(concern=>({params:{slug:concern.slug},props:{concern}}));}
export function GET({props}:{props:{concern:(typeof concerns)[number]}}){const {concern}=props;const rows=[['chart','label','value','unit','period_or_source'],...concern.dossier.series.labels.map((label,index)=>['dossier',label,String(concern.dossier.series.values[index]),concern.dossier.series.unit,concern.dossier.source.url])];for(const section of concern.investigation?.sections??[]){if(section.chart)section.chart.labels.forEach((label,index)=>rows.push([section.id,label,String(section.chart!.values[index]),section.chart!.unit,section.chart!.source.url]));}const csv=rows.map(row=>row.map(value=>`"${String(value).replace(/"/g,'""')}"`).join(',')).join('\n');return new Response('\uFEFF'+csv,{headers:{'Content-Type':'text/csv; charset=utf-8','Content-Disposition':`attachment; filename="${concern.slug}-datos.csv"`}});}
