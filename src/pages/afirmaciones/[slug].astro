---
import { claims } from '../../data/claimCatalog';
export function getStaticPaths() { return claims.filter((claim) => claim.published).map((claim) => ({ params: { slug: claim.slug } })); }
return Astro.redirect(`/comprobar/${Astro.params.slug || ''}`, 301);
---
