# Create .cloudflare.env beside this file and add your temporary Cloudflare token.
# Never commit or paste the real token into chat.
CLOUDFLARE_API_TOKEN=
# Optional shared secret for the Pages Function → local resolver connection.
LOCAL_CLASSIFIER_TOKEN=
